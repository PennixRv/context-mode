# Issue 009 Doctor Runtime Facts Implementation Plan

## 1. Establish Red Evidence

- Extend the built-entry-point fixture to project the MCP child environment from
  `.codex-plugin/mcp.json` instead of reusing the CLI environment.
- Assert the current source manifest omits the required Codex discovery capability and capture the
  failing CLI/MCP field comparison in task results without recording real profile data.
- Add focused table cases for inventory unavailable, not listed, disabled, missing cache manifest,
  stale release, runtime manifest/Hook defects, and host-session state unavailable.

Rollback point: tests only; no product behavior changed.

## 2. Implement The Observation Contract

- Extend Codex diagnostic observations and Plugin-list probes with closed, machine-readable reason
  values; keep free-form process errors and stderr out of serialized output.
- Project each required field from explicit source facts and preserve compatibility top-level
  values.
- Add a Codex Doctor report that derives Hook/config checks, structured JSON, and registration from
  one diagnostic snapshot.
- Teach CLI Doctor and MCP `ctx_doctor` to consume the shared report while retaining fallback
  behavior for other adapters.
- Render unavailable capability as informational/unavailable, not as installation failure; retain
  warnings and repair actions for proven disabled, missing, corrupt, or stale states.

Rollback point: diagnostic provider, adapter interface, CLI, and MCP report changes can be reverted
without touching execution/indexing code.

## 3. Forward Minimal Non-Sensitive Capabilities

- Add the confirmed `PATH`, `HOME`, and `CODEX_HOME` names to source Plugin `env_vars`, preserving
  the five presentation names and fixed platform marker.
- Update source, marketplace-builder, offline-install, and manifest-security tests to require exact
  parity and reject credential-like names.
- Build the marketplace twice and compare tracked/generated bytes and hashes for deterministic
  output.

Rollback point: manifest-only change can be reverted independently if the host rejects one of the
new names; no value is embedded and no installed cache is modified.

## 4. Documentation And Specification

- Update Doctor documentation with field sources, availability/reason semantics, and root/session
  distinctions.
- Update `.trellis/spec/backend/codex-diagnostics-and-test-routing.md` with the executable
  environment projection and one-snapshot contract.
- Record red/green evidence, commands, measured output, and root handoff in task results.
- Run `trellis-check`, then `trellis-update-spec` and retain only durable contract changes.

## 5. Validation

Focused checks:

```bash
pnpm exec vitest run tests/adapters/codex-diagnostic-projection.test.ts \
  tests/adapters/codex.test.ts tests/plugins/codex-doctor-entrypoints.test.ts \
  tests/plugins/codex-manifest.test.ts tests/codex/marketplace-layout.test.ts
pnpm run typecheck
node --check hooks/pretooluse.mjs
node --check hooks/codex/pretooluse.mjs
```

Repository gates (use exact script names discovered from `package.json` and release docs):

```bash
pnpm run format:check
pnpm run lint
pnpm run typecheck
pnpm run build
pnpm test
pnpm run bundle:check
pnpm run build:codex-marketplace
pnpm run verify:codex-marketplace
pnpm pack --dry-run
```

- Run the repository's asymmetric build/source drift checks and package-content verifier.
- Perform an offline temporary marketplace install and invoke both bundled CLI Doctor and MCP
  Doctor with synthetic Plugin inventory; verify all version-bearing files are one release.
- Repeat the marketplace build and confirm no unexpected tracked or byte drift.
- Inspect `git diff --check`, ignored/untracked output, credential-like strings in changed files,
  and final worktree status.

## 6. Source Commit And Release

- Confirm `1.0.189` and `v1.0.189` are not present locally, remotely, on npm, or in GitHub Releases.
  Stop rather than overwrite if occupied; choose the next free patch only according to repository
  convention.
- Synchronize all version files and release notes, run the complete gate again, and create the clean
  release source commit on `devel`.
- Run the disposable Codex native release preflight from that exact source commit using only the
  approved local provider projection and inherited authorization. Never print or persist provider
  authorization in the repository.
- Commit only `docs/releases/attestations/v1.0.189.json` as the direct child evidence commit.
- Create an annotated `v1.0.189` tag on the evidence commit with the exact content-manifest and
  attestation metadata required by the verifier.
- Push `devel`, then the single tag. Do not force-push, move tags, or publish a prerelease.
- Wait for CI and Release completion. Verify GitHub assets, npm package, marketplace archive,
  content manifest, provenance, tag object, peeled commit, source commit binding, and every
  SHA-256.

## 7. Handoff And Finish

- Give the root workflow the transaction-safe install source/version and state that a Codex restart
  is mandatory before comparing entry points.
- Provide the exact bundled CLI Doctor and MCP `ctx_doctor` comparison commands and expected field
  semantics without installing locally in this session.
- State that component acceptance is complete but root Issue 009 remains open until root installs,
  restarts, and verifies the real same-session comparison.
- Archive the completed Trellis task only after evidence, release, and handoff are recorded; leave
  the component repository clean.
